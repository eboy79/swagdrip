import { gsap, ScrollTrigger } from 'gsap/all';
gsap.registerPlugin(ScrollTrigger);

/**
 * Initialize the navigation menu.
 * @param {Object} [shaderAnim] - Optional shader animation instance controlling the logo.
 */
export function initializeNavigation(shaderAnim) {
   
}
